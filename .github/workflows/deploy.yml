name: Trigger Jenkins Deployment

on:
  push:
    branches: [ main ]

jobs:
  trigger-jenkins:
    runs-on: ubuntu-latest
    steps:
      - name: Call Jenkins API
        run: |
          curl -X POST "http://10.2.2.161:8080/job/myapp-deploy/build?token=${{ secrets.JENKINS_JOB_TOKEN }}"
